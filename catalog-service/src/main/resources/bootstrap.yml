spring:
    application:
        name: catalog
    cloud:
        config:
            discovery:
                enabled: true
                serviceId: config
    profiles:
        active: testing
eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:10001/eureka/,http://localhost:10002/eureka/